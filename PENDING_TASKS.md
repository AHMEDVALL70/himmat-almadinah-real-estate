# همة المدينة العقارية — المهام المعلّقة

## ⛔ قواعد عمل صارمة — اقرأها قبل أي شي

1. **ممنوع كتابة أو رفع أي سطر كود إلا بعد اتفاق صريح ومباشر مع المستخدم على الخطة أولاً.** لا افتراض موافقة ضمنية، ولا تنفيذ "لأنه واضح إنه المطلوب" — اسأل وانتظر رد صريح، حتى لو كان التغيير بسيط أو بديهي بنظرك.
2. **أي نقطة معلَّقة أو قرار مؤجَّل أو فكرة تُطرح ولا يُبت فيها فوراً، تُسجَّل بهذا الملف فوراً** بنفس الجلسة — لا تنتظر "لاحقاً" ولا تعتمد على الذاكرة.
3. **فكّر كمهندس، مو كمنفّذ أوامر:** قبل أي تعديل، اسأل نفسك: هل هذا يؤثر على أماكن ثانية بالكود؟ هل فيه أثر جانبي؟ هل السبب الجذري للمشكلة مفهوم فعلاً، أو أنا بس أعالج العرض الظاهر؟ اختبر منطقياً قبل الرفع، وتحقق من النشر فعلياً بعده (مو افتراض النجاح). الاستعجال في التنفيذ بدون هالتفكير هو سبب أغلب الأخطاء المتكررة اللي صارت بهالمشروع.

آخر تحديث: هذا الملف هو المرجع الوحيد للمهام المفتوحة. أي محادثة جديدة (مع Claude أو غيره) تقدر ترجع له بدل إعادة شرح كل شي من الصفر.

## ⚠️ فخ متكرر بلوحة Supabase — Edge Functions

زر **"Deploy a new function"** (أو أي محرر كود يظهر تلقائياً بصفحة "Create new edge function") **دائماً يسوي دالة منفصلة جديدة باسم عشوائي** (`clever-api`, `smart-endpoint`, `dynamic-service`...)، حتى لو نيتك تعديل دالة موجودة. صار هذا اللبس أكثر من مرة بنفس الجلسة (نسخ محدَّثة اتحطت بدوال زايدة بدل الدالة الصحيحة). **الطريقة الصحيحة دايماً لتعديل كود دالة موجودة:** من قائمة Edge Functions، اضغط **اسم الدالة نفسه** بالجدول (مو أي زر أخضر) → تبويب Code → Edit → استبدل → Deploy.

## 🔴 مشكلة بنيوية عاجلة — الموقع "فاضي" لأدوات الذكاء الاصطناعي (لسا قائمة)

**المشكلة:** الموقع بالكامل يُبنى بجافاسكربت من طرف المتصفح (Client-Side Rendering) — الكود الخام (HTML) يحتوي عناصر فاضية وعدّادات بقيمة "0" حتى يشتغل الجافاسكربت. المتصفحات العادية تشغّله تلقائياً وتشوف كل شي صح؛ أدوات الذكاء الاصطناعي اللي تسحب الكود الخام بدون تشغيل جافاسكربت تشوف بس أصفار وصناديق فاضية.

**السبب الجذري:** GitHub Pages استضافة ثابتة بالكامل — ما تشغّل أي كود خادم.

**الحل الموصى به:** وكيل ذكي (Cloudflare Worker) يعترض الطلب ويولّد صفحة ببيانات حية من Supabase للزوار الآليين فقط، والزائر العادي يشوف الموقع التفاعلي زي ما هو.

**الحالة:** ⏳ بانتظار المستخدم يجهّز دومين خاص (ما عنده واحد بعد) — بعدها نبدأ التنفيذ. **ملاحظة:** المستخدم عنده الآن حساب Cloudflare فعّال (من مشروع الذكاء الاصطناعي أدناه)، فالخطوة المتبقية فعلياً هي الدومين بس.

## 🔧 تحسينات هندسية

| # | المهمة | الحالة |
|---|---|---|
| 1 | فحص استدعاءات الدوال يدوياً | ✅ خلص |
| 2 | فصل الملفات (CSS/JS/مكتبات لملفات منفصلة) | ✅ خلص بالكامل، مؤكَّد شغّال |
| 3 | مراجعة أمنية شاملة (RLS) | ✅ خلص — راجع قسم "المراجعة الأمنية" أدناه للتفاصيل |
| 4 | PDF/طباعة للعقود | ✅ خلص — عبر طباعة المتصفح (`window.print()`) بقالب مصمَّم، بدل مكتبة PDF خارجية (تجنّباً لمشاكل عرض العربي الشائعة بمكتبات PDF) |

## 🚀 مراجعة هندسية شاملة قبل شراء نطاق/استضافة (2026-09-10)

المستخدم طلب مراجعة شاملة للموقع (مو بس ميزات جلسة معيّنة) قبل قرار شراء نطاق. راجعنا: السيو، التحليلات، الوصولية، الأداء، جمع العملاء. **التقرير الكامل بصيغة ملف منفصل أُعطي للمستخدم مباشرة** (`مراجعة-هندسية-قبل-النطاق.md`) — إذا احتجت مرجعه بجلسة قادمة، اطلب من المستخدم يرفعه لك، أو أعد نفس المراجعة (الأقسام أدناه تلخّص أهم النتائج والحالة الحالية).

**✅ نُفِّذ فوراً (لا يحتاج نطاق):**
- **Google Analytics (GA4)** مربوط — Measurement ID: `G-HVJMR09T3R`. أول أداة قياس زيارات حقيقية للموقع (كان صفر تحليلات قبلها، بس تتبّع داخلي بسيط بقاعدة البيانات لا يقارَن). إشارة شفافية أُضيفت بسياسة الخصوصية.
- **`robots.txt`** — أُضيف (كان غير موجود إطلاقاً). حالياً بسيط (`Allow: /` للجميع) بدون إشارة sitemap لأنه غير موجود بعد.
- **Favicon كامل** — كان غير موجود إطلاقاً. اقتُصّت أيقونة البرج الذهبي من الشعار (بدون النص، غير مقروء بالأحجام الصغيرة) وولّدنا كل الأحجام (`favicon.ico`, 16/32px, apple-touch-icon 180px, 192/512px) + `site.webmanifest` لدعم إضافة الموقع للشاشة الرئيسية بالجوال بأيقونة احترافية. الملفات بمجلد `icons/` بجذر المستودع.

**⏳ لسا معلّقة من نفس المراجعة (بالترتيب المقترح):**
1. **بيانات مهيكلة (Schema.org / JSON-LD)** لنوع `RealEstateAgent` — قيمتها الحقيقية مرتبطة بحل مشكلة الرؤية الآلية (🔴 أول نقطة بالملف) — نفّذها بعدها مباشرة.
2. **تحميل كسول لمكتبة Chart.js** (208 كيلوبايت) — تتحمّل حالياً لكل زائر رغم إنها تُستخدم بس بقسم "التحليلات" المخفي افتراضياً (`display:none` بـ`index.html`). تحسين أداء سهل وحقيقي، لم يُنفَّذ بعد.
3. **فجوات وصولية بسيطة** — لاحظنا بأدوات DevTools تحذيرات متكررة "no label associated with a form field". فحص سريع: 63 `<label>` مقابل 65 حقل إدخال بـ`index.html` — فرق بسيط يستاهل مراجعة.
4. **آلية جمع بريد إلكتروني (Newsletter)** — التواصل حالياً واتساب فقط، ما يبني قائمة عملاء للتسويق المستقبلي.
5. **محتوى/مقالات (Blog)** — لجذب زيارات بحث عضوية، قيمتها الحقيقية أيضاً مرتبطة بحل مشكلة الرؤية الآلية.

**تذكير مهم:** المستخدم شدّد إن أولوية حل مشكلة الرؤية الآلية تبقى **فور توفر النطاق مباشرة**، مو مؤجَّلة بعده — راجع النقطة 🔴 بأول الملف.

## 🤖 المساعد الذكي (Chatbot) — مختبَر ومؤكَّد شغّال (2026-09-09)

**الحالة النهائية:** مربوط بـ Gemini API الحقيقي عبر خادم وسيط آمن (Cloudflare Worker)، مو مساعد قواعد ثابتة. **اختُبر مباشرة مع المستخدم اليوم وأُصلحت عدة مشاكل حقيقية اكتُشفت أثناء الاختبار.**

**البنية:**
- `himmat-worker.js` (كود المستخدم نفسه بحساب Cloudflare، مو بمستودع GitHub) — يستقبل السؤال، يرسله لـ Gemini API (`gemini-3.6-flash`، `thinkingLevel: "low"` — راجع أحدث نموذج متاح دورياً)، يرجّع الرد. المفتاح (`GEMINI_API_KEY`) محفوظ كـ"سرّ" على Cloudflare فقط.
- حماية: CORS مقيَّد على `https://ahmedvall70.github.io` حصراً، حد طلبات 20/ساعة لكل IP (عبر KV Storage)، حد حجم رسالة/محادثة.
- الرابط: `https://himmat-ai-backend.ahmedvall.workers.dev/chat`.
- **3 طبقات قبل ما نلجأ لـGemini إطلاقاً** (أسرع، مجاني، صفر مخاطرة اختلاق):
  1. **أسئلة مطابقة لقسم معروف** (تقييم/عقد/إضافة عقار/عروض/تواصل) — رد ثابت دقيق فوري، عبر مطابقة كلمات مفتاحية **مطبَّعة** (`normalizeArabicForMatch` بـ`site.js` — توحّد أشكال الهمزة/الألف المقصورة/التاء المربوطة/التشكيل قبل المقارنة، فما يهم إملاء الزائر بالضبط).
  2. **أسئلة "أغلى/أرخص حي"** — تُحسب مباشرة من بيانات `district_prices` بالمتصفح (فرز رقمي بسيط)، بدون أي اتصال بـGemini.
  3. **بقية أسئلة الأسعار** (حي محدَّد) — تروح لـGemini مع بيانات `district_prices` الحقيقية مرفقة (مفلترة للمدينة لو مذكورة).
- **طبقة أخيرة داخل Gemini نفسه:** لو السؤال إجرائي عن أحد الأقسام الخمسة بصياغة/مرادف ما تغطيه الكلمات المفتاحية (مثل "أبي أسجل عقاري")، `SYSTEM_PROMPT_CHAT` يوجّه Gemini يرجّع إشارة `SITE_SECTION:<key>` بدل ما يحاول يشرح بنفسه — `site.js` يكتشفها ويستبدلها بالرد الثابت الدقيق دايماً.

**دروس مهمة (لأي جلسة قادمة تلمس هالجزء):**
1. **اسم النطاق الفرعي بـ Cloudflare** (`ahmedvall`) يجب التحقق منه دائماً من لوحة Cloudflare مباشرة، مو من ذاكرة المستخدم.
2. **رقم إصدار `site.js` بـ `index.html` لازم يتزوّد بكل تعديل** — نسيانه يخلي التعديلات "تختفي" (كاش) بدون أي رسالة خطأ.
3. **مطابقة نصية حرفية بالعربي خطرة بدون تطبيع** — "اضيف" (بدون همزة) ما تطابق "أضيف"/"اضافة" حرفياً رغم إنها نفس الكلمة عملياً. الحل: `normalizeArabicForMatch()` يوحّد الأشكال قبل أي مقارنة، بدل تعداد كل صيغة إملاء يدوياً.
4. **سطور "تشخيص مؤقت" منسية خطيرة** — لقينا سطرين يعرضون تفاصيل تقنية داخلية (رسالة خطأ Gemini الخام، بيانات تصحيح) مباشرة للزائر العادي، رغم إن التعليق فوقهم بالضبط يقول العكس. أي سطر فيه "مؤقت" أو "debug" لازم يتراجع عنه قبل أي نشر نهائي.
5. **بطء/فشل Gemini المتكرر (>20 ثانية أو 429)** — سببين مختلفين لقيناهم: (أ) `gemini-3.6-flash` يفعّل "تفكير" داخلي متوسط تلقائياً يرفع زمن الاستجابة — عالجناه بـ`thinkingLevel: "low"`. (ب) **حصة Google AI Studio المجانية منخفضة جداً لهالحساب** (RPM وRPD قريبين من الصفر لـ`gemini-3.6-flash` تحديداً) — راجع قسم "⏳ معلّق" أدناه.
6. **نموذج Gemini يتغيّر باستمرار** — تحقق دايماً من أحدث نموذج متاح ومن حصته المجانية بلوحة https://aistudio.google.com/rate-limit قبل الاعتماد عليه بجلسة قادمة.

## ⏳ معلّق — بانتظار قرار/إجراء من المستخدم

| # | الموضوع | التفاصيل |
|---|---|---|
| 1 | تفعيل الفوترة (Billing) على Google AI Studio | حصة `gemini-3.6-flash` المجانية منخفضة جداً وتُستهلك بسرعة. **تصحيح مهم:** الـ300$ اللي واجهها المستخدم أول مرة (2026-09-10) **مو رسوم أو عائق حقيقي** — هو عرض ائتمان ترحيبي مجاني من Google لمنتجات Cloud ثانية، **وينص صراحة إنه يستثني Gemini API**. الخطوة الفعلية بسيطة: إضافة وسيلة دفع فقط، والمحاسبة تكون على الاستخدام الفعلي (أقل من سنت للسؤال الواحد تقريباً). **لسا معلّقة** (المستخدم ما فعّلها بعد)، لكن **الخطر الأمني المصاحب لتفعيلها انخفض** بعد إضافة Cloudflare Turnstile (راجع القسم أدناه) — صار احتمال استنزاف الرصيد من بوت/سكربت أقل بكثير. **أي جلسة قادمة:** اسأل المستخدم أول شي هل قرر يفعّلها أو لا. |
| 2 | تسجيل دخول فريق العمل برقم الجوال (SMS) | الواجهة والكود (`manage-admin-users`) **جاهزين بالكامل** لدعم إنشاء حساب ودخول برقم جوال — لكن الإنشاء الفعلي هيفشل لين يفعّل المستخدم مزوّد SMS (Twilio أو شبيهه، مدفوع) من Supabase Dashboard → Authentication → Providers → Phone. البريد واسم الدخول (بدون بريد فعلي) يشتغلون فوراً بدون أي إعداد إضافي. |

## 🛡️ حماية المساعد الذكي من إساءة الاستخدام (Cloudflare Turnstile) — خلص ومختبَر (2026-09-10)

**السياق:** أثناء نقاش تفعيل فوترة Gemini، المستخدم سأل سؤالاً محورياً: هل أي زائر يقدر يستنزف الرصيد أو يسرق مفتاح Gemini من كود المتصفح؟ الجواب: **سرقة المفتاح مستحيلة** (موجود فقط على Cloudflare كسرّ، ما يظهر بكود الموقع إطلاقاً — هذا كان سبب بناء الـWorker الوسيط من الأصل). **لكن استنزاف الرصيد كان خطراً حقيقياً**: حماية CORS الحالية (`Access-Control-Allow-Origin`) **ما تمنع إطلاقاً استدعاءً مباشراً بأداة مثل curl** (بدون متصفح) — هي تتحكم بالمتصفح بس، مو حاجز حقيقي. حد الـ20 طلب/ساعة لكل IP كان يخفّف الخطر بس ما يلغيه (يُتجاوز بـVPN/بوت نت متعدد الـIPs).

**الحل المطبَّق:** Cloudflare Turnstile (كابتشا خفية بالكامل، مجانية 100% وبدون حد استخدام عملي — تأكدنا من هذا بالبحث). Site Key عام بكود `site.js`، Secret Key محفوظ كسرّ بالـWorker (`TURNSTILE_SECRET_KEY`) — نفس نمط `GEMINI_API_KEY` بالضبط. كل رسالة للمساعد الذكي تولّد توكن جديد يتحقق منه الـWorker مع Cloudflare قبل أي اتصال بـGemini؛ لو التحقق فشل، يُرفض الطلب فوراً بدون أي تكلفة.

**دروس/ملاحظات لأي جلسة قادمة:**
- لوحة Cloudflare فيها باق معروف (مو خاص بنا — شفناه بمنتدى Cloudflare الرسمي لمستخدمين ثانيين بنفس اليوم): إضافة Hostname بنموذج "Add Widget" ممكن ما تنحفظ لو ضغطت اقتراح "Add as custom hostname" بسرعة زيادة قبل ما تختفي دائرة التحميل الصغيرة. الحل: انتظر ثانية-ثانتين قبل الضغط.
- **فخ ثاني بلوحة Cloudflare (نفس فئة فخ Edge Functions المذكور بالأعلى):** إذا نسيت تنسخ الـSecret Key وقت الإنشاء، **لا تروح لصفحة تعديل سرّ ثاني موجود أصلاً بالغلط** (زي `GEMINI_API_KEY`) — روح تحديداً لودجت Turnstile نفسه واستخدم خيار "Rotate Secret Key" لتوليد وحد جديد بأمان.
- توكن Turnstile الواحد يُستخدم مرة وحدة بس — لازم ودجت جديد (`turnstile.render`) لكل رسالة، مع `turnstile.remove()` **صريح** للودجت القديم قبل إنشاء الجديد (نسيان هذا يسبب تحذير "Cannot find Widget" بالكونسول — تسريب موارد بسيط، مو خطأ يوقف الشات، لكن يستاهل تصحيح).
- **اكتشاف جانبي مهم أثناء الاختبار:** Gemini كان يستخدم كلمة "التقييم" بردوده التلقائية (رغم كل جهد الامتثال النظامي بالموقع نفسه) — لأن `SYSTEM_PROMPT_CHAT` ما كان يمنعها صراحة. أُضيفت تعليمات صريحة تمنع "تقييم"/"تقدير" وتفرض "المؤشر" دايماً. **درس عام:** أي تغيير مصطلحات نظامي بالموقع لازم يتفحّص هل الذكاء الاصطناعي (اللي يولّد نص حر) يحتاج تحديث تعليماته هو كمان، مو بس نصوص الموقع الثابتة.
- ودجت الودجت اسمه "Himmat Chat"، يدعم حتى 10 دومينات بنفس الـSite Key — لو المستخدم جهّز دومين خاص مستقبلاً (مثل himmat.sa)، يضيفه لنفس الودجت من Hostname Management، بدون أي تعديل كود.

## 🔑 نظام صلاحيات الفريق (owner / editor / viewer) — خلص ومختبَر (2026-09-09)

**الحالة:** نظام أدوار كامل، مبني بعد اكتشاف ثغرة أمنية حقيقية (راجع "🔐 المراجعة الأمنية" أدناه).

**3 أدوار:**
- **owner** — كل شي بدون استثناء (بما فيها إضافة مدن/أحياء وإدارة الفريق نفسه). المستخدم الحالي هو الوحيد بهذا الدور.
- **editor** — كل عمليات التشغيل اليومي (اعتماد/رفض/حذف عقارات، عروض، استفسارات، تعليم دفعات مدفوعة) **إلا** إضافة مدن/أحياء وإدارة الفريق.
- **viewer** — قراءة/مشاهدة بس، صفر كتابة بأي مكان.

**آلية الحماية (مهمة لأي تعديل مستقبلي):**
- الدور محفوظ بحقل `app_metadata.role` بحساب Supabase Auth — حقل **ما يقدر المستخدم نفسه يعدّله** (يحتاج مفتاح `service_role`)، عكس `user_metadata` اللي أي حساب يقدر يغيّره بنفسه ويرفّع صلاحياته بخداع.
- دالتين بقاعدة البيانات: `is_owner()` (owner بس — تحكم بصلاحيات المدن/الأحياء)، و`is_staff()` (owner أو editor — تحكم بكل صلاحيات RLS اليومية). **أي جدول/صلاحية كتابة جديدة مستقبلاً لازم تستخدم وحدة من الدالتين، مو `auth.role() = 'authenticated'` مباشرة** (هذي الأخيرة تسمح لأي حساب حتى لو viewer).
- بالواجهة (`admin.js`/`admin.css`): `document.body.dataset.role` يُضبط بعد تسجيل الدخول، وأي عنصر عليه `data-staff-only` يختفي لـviewer، وأي عنصر عليه `data-owner-only` يختفي لـviewer وeditor. **هذا حماية واجهة بس (تجربة استخدام)** — الحماية الحقيقية دايماً بقاعدة البيانات (RLS) عبر `is_owner()`/`is_staff()`.
- إدارة الفريق نفسها (`manage-admin-users` Edge Function) لا تعتمد على RLS إطلاقاً — تتحقق من دور الطالب يدوياً بالكود (owner حصراً لكل الإجراءات: إضافة/حذف/تعطيل/تفعيل/تغيير كلمة مرور عضو).

**دعم 3 صيغ دخول:** بريد إلكتروني عادي، اسم دخول (يتحوّل داخلياً لبريد وهمي بنطاق `@team.himmat.local` ما يستقبل رسائل حقيقية أبداً)، أو رقم جوال (معلّق لين SMS — أعلاه).

## 🗺️ إدارة المدن والأحياء من لوحة التحكم — خلص ومختبَر (2026-09-09)

**الحالة:** الإدارة (owner بس) تقدر تضيف مدينة أو حي جديد مباشرة من تبويب "المدن والأحياء" بلوحة التحكم، بدون أي تدخل كود أو رفع مستقبلي.

**نقطة تاريخية مهمة:** هذي الميزة (تحديداً "إضافة مدينة" من واجهة الموقع) **كانت موجودة سابقاً** (أُضيفت 2026-09-01 مساءً)، ثم **انحذفت بعدها بـ3 ساعات فقط** بنفس اليوم أثناء تحويل حقول الحي من كتابة حرة لقوائم منسدلة — برسالة كوميت عامة غير واضحة ("Update print statement...") ما تشرح الحذف إطلاقاً. **الدرس:** رسائل commit غامضة أو افتراضية تفقد تاريخ المشروع فعلياً — أي حذف لميزة (حتى لو جزء من ريفاكتور أكبر) لازم يُذكر صراحة برسالة الكوميت.

**كيف تشتغل الآن:**
- جدول `districts`/`cities` بقاعدة البيانات هو **المصدر الوحيد** — الموقع العام (`site.js`, دالة `loadCitiesFromDb`) يقرأ منه مباشرة عند كل تحميل صفحة، ويدمجه مع قائمة ثابتة احتياطية بالكود (لو قاعدة البيانات تعطلت مؤقتاً).
- إضافة حي جديد يستفيد **تلقائياً** من التحديث الأسبوعي لأسعار الأحياء (كل أحد 3 فجراً) — بدون أي تعديل كود، لأن الدالة تجيب كل الأحياء من الجدول مباشرة (مو قائمة ثابتة).
- عمود `raghdan_slug` بجدول `cities` (بدل خريطة ثابتة كانت بكود الدالة) — يخلي حتى **مدينة جديدة كاملة** تشتغل تلقائياً مع التحديث الأسبوعي، بشرط تعبئة الحقل صح وقت الإضافة.
- **تذكير:** نجاح تسعير حي جديد تلقائياً **غير مضمون** — يعتمد على وجود بيانات كافية له بموقع raghdan.sa (نفس وضع الأحياء الأخرى بدون سعر، راجع القسم أدناه).

| # | الفكرة | الحالة |
|---|---|---|
| 1 | خريطة تفاعلية بدبابيس للعقارات | ⏳ يحتاج إحداثيات (خط طول/عرض) لكل عقار — غير متوفرة |
| 2 | معاينة صور متعددة لكل عرض | ✅ خلص (2026-09-10) — عمود `image_urls` جديد (`image_url` يبقى صورة الغلاف)، رفع عدة صور دفعة وحدة بلوحة التحكم مع شريط مصغّرات قابلة للحذف، ومعرض صور بأسهم وعداد بنافذة التفاصيل بالموقع العام |
| 3 | معرض آراء عملاء حقيقية | ⏳ ما عندنا آراء مجمّعة بعد |
| 4 | ضغط صور الموقع/الشعار لصيغة WebP | ✅ خلص (2026-09-10) — الشعار تحديداً: 140KB → 17KB (جودة 90، تقليل 87%)، مع `<picture>` وPNG احتياطي لأي متصفح قديم. `og:image`/`twitter:image` بقيت PNG (توافق أوسع مع معاينات المشاركة) |
| 5 | توليد وصف تسويقي بالذكاء الاصطناعي بلوحة التحكم | ✅ خلص (2026-09-10) — زر "توليد بالذكاء الاصطناعي" بنموذج العروض، يستخدم مسار `/describe` بالخادم الوسيط مع حماية Turnstile (أضيفت للوحة التحكم كمان لهذا الغرض) |
| 6 | Minify لملفات JS/CSS | ⏳ مؤجَّلة عمداً لين استقرار الموقع (يحتاج خط أنابيب بناء حقيقي) |
| 7 | sitemap.xml | ⏳ قيمته محدودة طالما مشكلة الرؤية الآلية قائمة |

## 🔐 المراجعة الأمنية — النتيجة النهائية

**🔴 ثغرة حقيقية اكتُشفت وأُصلحت (2026-09-09):** إعداد "Allow new users to sign up" بـSupabase كان **مفعّلاً** (الافتراضي)، وبما إن أي حساب Supabase Auth ثاني كان ياخذ نفس الصلاحيات الكاملة تلقائياً (قبل نظام الأدوار أدناه)، كان أي زائر تقني بسيط يقدر يسجّل حساب لنفسه (عبر `supabase.auth.signUp()` من الكونسول مباشرة، حتى بدون زر تسجيل بالواجهة — مفتاح anon موجود أصلاً بكود الموقع العام) ويوصل لصلاحيات إدارة كاملة. **تم تعطيل الإعداد فوراً**، وتأكدنا من قائمة المستخدمين (حساب واحد بس، بريد المستخدم نفسه) إن الثغرة **ما استُغلّت**. أي جلسة قادمة: تأكد هذا الإعداد لسا معطّل (Authentication → Sign In / Providers).

فحصنا كل الـ19 تحذير من Supabase Security Advisor:
- **4 تحذيرات RLS "دائماً صحيح"** — إيجابيات كاذبة، مصمَّمة عمداً (قراءة عامة لبيانات مرجعية غير حساسة `cities`/`districts`/`district_prices`، أو إدخال عام فقط بدون قراءة/تعديل `properties`/`inquiries`/`page_views`/`offer_views`).
- **دوال بدون `search_path` محدَّد** — ✅ أُصلحت الأربعة كلها (`force_pending_on_insert`, `recalc_contract_total`, `generate_installments`, `create_contract_with_schedule`) بإضافة `set search_path = public, pg_temp`.
- **تخزين صور العقارات يسمح بعرض قائمة الملفات** — خطورة منخفضة (صور تسويقية أصلاً مقصود عرضها للجميع)، قرار واعٍ بعدم التعديل.
- **دالة العقود (SECURITY DEFINER) متاحة للعموم** — بالتصميم (لازم تكون متاحة لزوار غير مسجَّلين)، محمية بقيود قاعدة البيانات (تواريخ منطقية، حقول إلزامية).
- **حماية كلمات المرور المسرَّبة** — تحتاج خطة Supabase **Pro** المدفوعة (جُرِّبت، رُفضت من الخطة المجانية الحالية). قرار مؤجَّل لأنها تحمي بس حساب الإدارة نفسه، خطورة منخفضة مع كلمة مرور قوية.

## 🏢 ربط إيجار الحكومي

✅ التسجيل الرسمي كمنصة تسويق عقاري لدى إيجار **تم فعلياً**. البنية التحتية جاهزة بقاعدة البيانات — رقم الصك وتاريخه، ونوع الهوية والجنسية لكل من المؤجر والمستأجر، **كلها حقول إجبارية فعلياً بنموذج العقد الآن** (تحقق أمامي بملف `js/site.js`؛ سطر سابق بهذا الملف كان يذكر نوع الهوية والجنسية كإجباريين قبل أوانه — تم تصحيحه). ⏳ ننتظر: هل عند المستخدم وصول لبوابة مطورين (Developer Portal) أو وثائق API رسمية من إيجار؟ آخر تحديث: المستخدم لسا يراجع. **أي جلسة قادمة: اسأل المستخدم أول شي هل راجع وحصل شي.**

## 📍 الأحياء بدون سعر مسجَّل (~75 حي بآخر تشغيلة، قد يتغيّر أسبوعياً)

راجع جدول `district_prices` مباشرة بقاعدة البيانات للقائمة المحدَّثة — تتغيّر أسبوعياً مع كل تشغيلة تلقائية (كل أحد 3 فجراً، أو يدوياً عبر زر "Test" بصفحة دالة `update-district-prices` بلوحة Supabase). لا تعتمد على قائمة ثابتة بهذا الملف لأنها تصبح قديمة بسرعة. معدّل النجاح المعتاد ~72%، مستقر رغم كل تعديلات اليوم (191/266 بآخر تشغيلة فعلية). أسباب الفشل نوعين: "لم يُعثر على نمط السعر" (raghdan.sa ما عنده بيانات كافية لهالحي) أو خطأ اتصال شبكة مؤقت (عادة يُحل تلقائياً بالتشغيلة التالية).

**⏳ بُحث بعمق (2026-09-10) — معلّق مفتوح، بلا حل آلي حالياً.** المستخدم سأل عن طريقة "معتمدة" لتسعير الأحياء الفاشلة. فحصنا **4 مصادر رسمية/شبه رسمية** بالتفصيل:
1. **REGA — منصة "المؤشرات العقارية"** (`rei.rega.gov.sa`، نفس جهة ترخيص الموقع بالـFAL): تطبيق JS معقّد، ومؤشراتها للبيع **نسبية** (تغيّر % من سنة أساس)، مو رقم "ريال/م²" مطلق. حتى بياناتها المفصَّلة (الإيجار) رجعت "No data available" لأحياء معيّنة — دليل مباشر إن المصدر الجذري (وزارة العدل) نفسه ناقص هالبيانات.
2. **البورصة العقارية السعودية** (`srem.moj.gov.sa`): **تمنع صراحة أي وصول آلي (robots.txt)** — هذا حظر مقصود من وزارة العدل، مو مجرد حماية Cloudflare عادية. **قرار واعٍ بعدم محاولة تجاوزه إطلاقاً** — تجاوز حظر جهة حكومية (خصوصاً وموقعنا مرخّص من REGA رسمياً) مخاطرة نظامية حقيقية (نظام مكافحة الجرائم المعلوماتية) غير متناسبة أبداً مقابل سدّ فجوة بيانات أحياء قليلة.
3. **بوابة ناجز مطورين** (`developers.najiz.sa`، قسم "البورصة العقارية"، 18 API): كلها APIs لسجل **الصكوك والملكية والرهن** (استعلام/تحديث صك، رهن، حجز)، **صفر منها يوفّر إحصائيات أسعار**. مصمَّمة لتوثيق الملكية، مو لمؤشرات السوق.
4. **Web Scraping متقدّم (بروكسيات) لموقع وزارة العدل**: اقترحه المستخدم — **مرفوض بوضوح** لنفس سبب #2 (حظر صريح + مخاطرة نظامية تفوق الفائدة بكثير).

**الخلاصة:** ما فيه حل آلي/برمجي متاح حالياً يسد الفجوة بأمان وشرعية. **لا تعيد اقتراح تجاوز حظر srem.moj.gov.sa بأي طريقة (بروكسي أو غيره) بأي جلسة قادمة — هذا خط أحمر واضح تم حسمه.**

**✅ الحل البديل (الإدخال اليدوي) خلص وشغّال فعلياً (2026-09-10).** قسم "تحديث سعر حي يدوياً" بتبويب "المدن والأحياء" (owner فقط) — يدخل نطاق سعري (أدنى/أعلى) من مصدر رسمي (REGA مثلاً)، والمتوسط بينهم يُستخدم كـ`price_per_sqm` عادي (نفس دور رقم راغدان بالضبط، بدون منطق حساب مختلف). `source='manual'` يوقف التحديث الأسبوعي التلقائي عن لمس هذا الحي تلقائياً (ما يُمحى)، مع زر "إرجاع للتحديث التلقائي" يشيل العلامة وقت ما يحتاجه المستخدم. الموقع العام يعرض ✅ ويذكر النطاق + ملاحظة المصدر بدل نص راغدان العام. **مُختبَر فعلياً من طرف المستخدم (حي الزهرة، المدينة المنورة، 1,800–2,200 ر.س) ونجح بالكامل من اللوحة للموقع العام.**

## ✅ خلص بالكامل (سجل تاريخي مختصر)

**تسمية/امتثال نظامي:**
- "التقييم" (وكل مشتقاتها) اتغيّرت لـ"المؤشر" بكل الموقع — "تقييم عقاري" مصطلح محمي نظامياً بالسعودية يتطلب ترخيص من الهيئة السعودية للمقيّمين المعتمدين.

**العقود:**
- رقم الصك وتاريخه أصبحا حقلين إجباريين (كانا اختياريين).
- نوع هوية المؤجر، جنسية المؤجر، نوع هوية المستأجر، جنسية المستأجر، وعدد الدفعات سنوياً — أصبحت كلها حقول إجبارية أيضاً (كانت اختيارية بالخطأ، تُصلح بالتحقق الأمامي بـ `js/site.js`).
- تحقق شامل قبل الإرسال، طباعة/PDF، جدول دفعات تلقائي، تحضير كامل لإيجار.

**لوحة التحكم:**
- تغيير كلمة المرور مباشرة من لوحة التحكم (بدون الحاجة لبريد استعادة من Supabase، اللي واجه مشكلة انتهاء صلاحية فورية بسبب فحص الأمان التلقائي لجيميل).
- تعليم دفعة "مدفوعة"، "تم البيع" للعروض، تحويل عقار معتمد لعرض تلقائياً.
- نظام صلاحيات فريق كامل (owner/editor/viewer) + قسم "فريق العمل" (إضافة/عرض/حذف/تعطيل/تغيير كلمة مرور، دعم بريد/اسم دخول/جوال) — تفاصيل كاملة بقسم مخصَّص أعلاه.
- قسم "المدن والأحياء" لإضافة مدينة/حي من لوحة التحكم مباشرة — تفاصيل كاملة بقسم مخصَّص أعلاه.
- تجاوب الجوال (2026-09-10) — الملف كان بدون أي استعلام وسائط إطلاقاً؛ أُصلح الشريط العلوي (تكديس عمودي)، شريط التبويبات (تمرير أفقي بدل التزاحم)، وجدول لوحة المعلومات الوحيد الناقص لحاوية تمرير. **مؤكَّد شغّال فعلياً بجهاز حقيقي.**
- إصلاح تغيير كلمة المرور (2026-09-10) — كان يفشل برسالة "Current password required..." بسبب إعداد أمني مفعَّل بـSupabase (Secure Password Change) يتطلب كلمة المرور الحالية. أُضيف حقلها. **درس عام:** أي استدعاء `supabase.auth.updateUser({password:...})` بمشروع مستقبلي لازم يتحقق أول من هالإعداد بالمشروع (Authentication → Providers → Email → Secure password change) قبل افتراض إنه يشتغل بدون كلمة مرور حالية.

**الموقع العام:**
- رابط تيك توك صُحِّح (كان ناقص رقم)، انستقرام كان صحيح أصلاً.
- تلميح "بوابة الإدارة" عند اقتراب الماوس من زر الدخول.
- معاينة الوصف الكامل بتمرير الماوس على زر "عرض التفاصيل الكاملة" بكرت العرض (2026-09-10) — صندوق مشترك `position:fixed` محسوب بجافاسكربت لتفادي قص `overflow:hidden` على `.offer-card`.
- تطبيع الأرقام العربية-الهندية للاتينية بحقول الهوية/الجوال، تعبئة الجنسية تلقائياً لو الهوية وطنية.
- حقوق النشر "جميع الحقوق محفوظة" بالفوتر.
- 5+ إصلاحات لغة/ترجمة (التقييم/التحليلات ما يتحدّثون عند تبديل اللغة، خطأ سباق بالآلة الكاتبة، ترجمة نوع العقار، تخطيط الإحصائيات بالإنجليزي).

**الأداء والبنية:**
- فصل الملفات الكامل (مكتبات + CSS + JS)، حذف مكتبة 624 كيلوبايت غير مستخدمة، تحميل كسول للتحليلات.

**من قبل:**
- تحديث أسعار الأحياء الأسبوعي (تلقائي، ~72% نجاح)، نظام مراقبة `job_runs`، إحصائيات زيارات حقيقية، إعادة تصميم الصفحة الرئيسية، حماية فرع `main`، ضغط الصور تلقائياً عند الرفع.
